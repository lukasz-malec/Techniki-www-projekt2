doctype html
html
  head
    link(rel="stylesheet" href="/css/styles.css")
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")

  body
    main.d-flex.justify-content-center.align-items-center.min-vh-100
      .register-container.p-4.border.border-warning.rounded-3.shadow-lg
        h2.text-center.text-warning.mb-4 Zarejestruj się
        form#registerForm(action="/register" method="POST")
          .mb-3
            label.form-label.text-warning(for="username") Nazwa użytkownika:
            input#username.form-control.bg-dark.text-light(type="text" name="username" placeholder="Wprowadź nazwę użytkownika" required)
          
          .mb-3
            label.form-label.text-warning(for="email") Email:
            input#email.form-control.bg-dark.text-light(type="email" name="email" placeholder="Wprowadź email" required)

          .mb-3
            label.form-label.text-warning(for="password") Hasło:
            input#password.form-control.bg-dark.text-light(type="password" name="password" placeholder="Wprowadź hasło" required)

          .mb-3
            label.form-label.text-warning(for="confirmPassword") Potwierdź hasło:
            input#confirmPassword.form-control.bg-dark.text-light(type="password" name="confirmPassword" placeholder="Potwierdź hasło" required)

          .mb-3
            input#privacyPolicy(type="checkbox" name="privacyPolicy" required)
            label.text-warning(for="privacyPolicy") Akceptuję 
              a(href="privacy_policy.html" class="text-warning") politykę prywatności

          button.btn.btn-warning.w-100(type="submit") Zarejestruj się

        .mt-3.text-center
          a(href="/logowanie" class="text-warning") Masz już konto? Zaloguj się

    script(src="/js/button.js")
